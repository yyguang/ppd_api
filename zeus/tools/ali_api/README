ali_api.py  获取阿里的色情结果，单线程版本
ali_api.py  使用方法: python ali_api.py image_list_path delimiter log_file_path
输出结果直接打印到屏幕，可以重定向到文件
第一列为路径,第二列为预测结果，第三列为预测的正常分，第四列为预测的色情分，第五列为性感分，第六列为真实结果，用"\t"分割
输入文件第一列为文件绝对路径，第二列为标签，用"\t"分割，标签只能为porn,normal,sexy
示例:sample.list
采用以下命令安装阿里云sdk:
1. 依赖的库在本工程 aliyuncs目录下, 包括两个库:
aliyun-python-sdk-core
aliyun-python-sdk-green(v20170112)
安装:
    sudo pip install aliyun-python-sdk-core==2.0.26 //安装指定版本
    sudo pip install -v aliyun-python-sdk-green==3.0.0 //安装指定版本
2.需要替换自己的ak到配置文件里面，见配置文件aliyun.ak.conf, 你也可以直接在程序里面替换
