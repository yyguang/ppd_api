拉数据教程：
sh download_daily.sh task_ids.txt result_dir
task_ids.txt文件示例
[465512,465513,465514,465515,465516,465508,465509,465510,465511]

注意：
当需要下载的数据为原始文件名而不是md5时（比如做数据修正时），需要修改download_daily.sh中的RENAME_MD5为0

recall_repeat_imgs.py
将重复图片加回到统计文件中，用于下一步的指标计算
python recall_repeat_imgs.py monthly-test-1802-sm-v20180211.txt repeat_img_list.txt

download_spark.py  根据spark分析结果下载数据

download_csv.py    从网页抓取得到csv后下载图像数据，并重命名为md5

rename_md5.py      将某个目录下所有图片按md5进行重命名

random_sample.py   对某个目录下的所有图片随机采样到新的目录


用于从指定url_list中下载对应的文件，并按照文件md5进行去重
用法:python download.py <url_list> <destination_path>
download_insert在去重的同时把自己的数据加到数据库中，用法同上

requirements:
需要安装futures和mysql
conda install futures
pip install mysql-python
如果报错 sh: mysql_config: command not found
需要先安装 yum install mysql-devel

md5_insert.py      python md5_insert.py full_path_list data_type remove_tag 用于存储数据集图片md5到数据库，full_path_list 是要插入的full_path的list，data_type是数据类型，remove_tag是一个可选参数，为1时重复的会删掉，为0时不会删除重复.

捞取标框数据, 修改其中的起始任务号。
python get_crop_result.py 433100,433105
